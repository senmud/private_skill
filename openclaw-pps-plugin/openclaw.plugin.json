{
  "id": "openclaw-pps",
  "name": "Feishu PPS Privacy",
  "description": "Per-session privacy policy for Feishu: strict vs owner-DM, tool interception, audit, owner notify",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "ownerOpenId": {
        "type": "string",
        "description": "Feishu user open_id (ou_xxx) for owner DM; used to classify OWNER_DM vs NON_OWNER_DM"
      },
      "auditPath": {
        "type": "string",
        "description": "Optional override for JSONL audit file path"
      },
      "notifyOwnerOnBlock": {
        "type": "boolean",
        "default": true,
        "description": "Send summary to owner DM when GROUP or NON_OWNER_DM triggers block/redact"
      },
      "debug": {
        "type": "boolean",
        "default": false,
        "description": "Enable verbose PPS debug logs for hook decisions and message emoji appending"
      },
      "feishuNotifyAccountId": {
        "type": "string",
        "description": "Optional Feishu account key under channels.feishu.accounts (defaults to defaultAccount or 'default')"
      }
    }
  }
}
